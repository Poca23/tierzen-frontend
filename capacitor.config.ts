import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tierzen.app',
  appName: 'TierZen',
  webDir: 'dist/tierzen-frontend/browser',
};

export default config;
